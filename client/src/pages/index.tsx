import { PagePlaceholder } from '../components/PagePlaceholder';

// Thin page wrappers. Each gets its real implementation in Week 2/3.
export const ProfilePage = () => (
  <PagePlaceholder titleKey="pages.profile.title" subtitleKey="pages.profile.subtitle" />
);
