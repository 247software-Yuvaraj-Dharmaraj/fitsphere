// @fontsource packages ship CSS-only entrypoints (no JS types), so declare them
// for the side-effect `import '@fontsource-variable/...'` calls in main.tsx.
declare module '@fontsource-variable/*';
