# Client build-time config.
# Local dev leaves this UNSET — Vite proxies /api to the server (see vite.config.ts).
# In production (Vercel), set this to your deployed API base URL:
#   VITE_API_URL=https://fitsphere-api.onrender.com/api
VITE_API_URL=
